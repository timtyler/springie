<?xml version="1.0" encoding="UTF-8"?>
<!--
  Springie logging configuration.

  The old home-grown Log class printed everything to stdout, so the root
  level stays at DEBUG to preserve that behaviour. Raise it (e.g. to INFO)
  to quiet the chatter.
-->
<configuration>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <target>System.out</target>
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="debug">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>
